# Python String

```python
# reverse string
rev_str = 'abcdefg'[::-1]
''.join(reversed('abcdefg')) # reversed('abcdefg') reversed object

# char is letter or digit
'c'.isalpha()
'c'.isdigit()

# convert to ascii from char or convert to char from ascii
chr(ord('a') + 3)

# lower or upper
'AbC'.lower()
'abc'.upper()

"a b c".split(separator, max)

myTuple = ("John", "Peter", "Vicky")
x = "#".join(myTuple) # John#Peter#Vicky

pq = [(-S.count(x), x) for x in set(S)] # enum # of char
```
